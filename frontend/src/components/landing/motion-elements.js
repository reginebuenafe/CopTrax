// Aliased framer-motion elements, referenced this way (instead of the
// <motion.div> member-expression JSX form) so this project's ESLint
// no-unused-vars rule correctly recognizes the `motion` import as used.
import { motion } from "framer-motion";

export const MotionDiv = motion.div;
export const MotionSpan = motion.span;
export const MotionP = motion.p;
export const MotionH2 = motion.h2;
export const MotionH3 = motion.h3;
export const MotionIframe = motion.iframe;
